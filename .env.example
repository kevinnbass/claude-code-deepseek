# Copy this file to .env and update the values with your API keys

# API Keys (At least one of DEEPSEEK_API_KEY or GEMINI_API_KEY is required)
#-----------------------------------------------------------------------
# Required for Sonnet models (complex reasoning, code generation)
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# Required for Haiku models (simple queries, fast responses)
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Only needed for comparison testing against real Claude models
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Model Configuration (Optional)
#-----------------------------------------------------------------------
# Model to use for Sonnet (complex tasks)
# BIG_MODEL=deepseek-chat

# Model to use for Haiku (simpler tasks)
# SMALL_MODEL=gemini-2.0-flash

# Advanced Configuration (Optional)
#-----------------------------------------------------------------------
# Set to any value to enable debug logging
# DEBUG=true